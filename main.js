
// IF STATEMENT

	// function verify() {
	// 	let age=document.getElementById('agex').value

	// 	if (age >= 18) {
	// 		window.alert("Verified! Thank you for your participation.")
	// 	}

	// }

// IF ELSE STATEMENT

	function verify() {
		let age=document.getElementById('agex').value

		if (age >= 18) {
			window.alert("Verified! Thank you for your participation.")

		} else {
			window.alert("Sorry, you're still underage! Try again in a few years.")
		}

	}